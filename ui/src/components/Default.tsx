import React from 'react';

const Default = () => (
    <div>
        <h1 className="title">Welcome to ShipMint</h1>
        <h2 className="subtitle">Select a customer from the menu to continue</h2>
    </div>
);

export default Default;
